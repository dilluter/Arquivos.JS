const cars = ["bmw", "audi", "mercedes", "toyota", "ford", "honda", "nissan", "chevrolet", "hyundai", "kia"];

let i = 0;
let text = "";
for (;cars.length[i];) {
    text += `${cars[i]} \n `;
    i++;
}